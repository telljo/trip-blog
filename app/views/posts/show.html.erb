<div class="container align-items-center d-flex flex-column" style="max-width: 50em;">
  <%= link_to trip_path(@post.trip), class: "btn btn-primary", style:"width:9em; align-self: end;" do %>
    <i class="bi bi-arrow-left"></i>
    Back to trip
  <% end %>
  <%= render @post %>
</div>